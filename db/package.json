{
  "name": "opencar-oem-db",
  "private": true,
  "scripts": {
    "export:csv": "node scripts/export-year-csvs.js",
    "build:db": "node scripts/build-oem-db.js",
    "build": "npm run build:db",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev",
    "prisma:studio": "prisma studio"
  },
  "devDependencies": {
    "prisma": "^6.19.0"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "csv-parse": "^7.0.1"
  }
}
